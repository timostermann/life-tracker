# SQLite database path
# - Host dev (recommended): keep the file in the repo (ignored) for easy debugging.
# - Docker prod (later in ticket-013): use /data/db.sqlite with a mounted volume.
DATABASE_PATH=./.data/db.sqlite

# Auth secret (used in ticket-003)
SESSION_SECRET=change-me
